apiVersion: v1
kind: Service
metadata:
  name: selfheal-service
spec:
  type: NodePort
  selector:
    app: selfheal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007